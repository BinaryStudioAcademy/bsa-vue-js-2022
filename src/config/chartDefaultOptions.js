const chartDefaultOptions = {
    responsive: true,
    maintainAspectRatio: false,
    legend: {
        display: false,
    },
    tooltips: {
        displayColors: false,
        callbacks: {
            title: () => {},
        },
    },
};

export default chartDefaultOptions;
